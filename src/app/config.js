export const FPS = 60
export const FRAME_INTERVAL = 1000 / FPS
export const DEVICE_PIXEL_RATIO = window.devicePixelRatio
export const SCREEN_WIDTH = window.innerWidth
export const SCREEN_HEIGHT = window.innerHeight
export const CAMERA_RADIUS = 100
export const CAMERA_ANGLE_INTERVAL = (2 * Math.PI) / 720
export const BOX_SIZE = 10
